name: Check aio-cli github action

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        node: [14]
        os: [windows-latest]

    steps:
    - name: Setup CLI
      uses: adobe/aio-cli-setup-action@v1
      with:
        os: ${{ matrix.os }}
        version: 6.0.0
    - name: info
      run: |
        aio info
